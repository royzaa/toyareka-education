import 'package:flutter/material.dart';

class Reportase extends StatelessWidget {
  const Reportase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Belum ada reportase"),
    );
  }
}
